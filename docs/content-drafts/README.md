# Content drafts

The author's own vegan-cooking notes, received 2026-09-10, not yet modeled into `data/recipes/` or
any other curated table. See `docs/IDEAS.md` §6 for what each one is, what it would take to model
correctly, and what has already been pulled in (the `ferment` lesson in `data/zero_waste.json` and
`data/regrow_tips.json` both draw on this material).

These are drafts, not curated data: nothing here is validated by `scripts/validate_recipes.py` or any
other validator, and nothing in `src/` reads from this directory.
